extends Node;

signal heatZoneEnter(zoneInstance: Node);
signal heatZoneLeave(zoneInstance: Node);

signal playerColdLevelChange(coldLevel);

signal itemSpawned(itemInstance);
signal inventoryInitialized();

signal showMessage(message);

signal usingUiChanged();
